{
  "name": "waku-workspace",
  "type": "module",
  "scripts": {
    "dev": "bun ./scripts/dev.ts",
    "release": "bun ./scripts/release.ts",
    "protocol:generate": "cargo run -p waku-protocol --bin export_types",
    "protocol:check": "cargo run -p waku-protocol --bin export_types -- --check",
    "db:generate": "drizzle-kit generate",
    "db:push": "drizzle-kit push"
  },
  "private": true,
  "trustedDependencies": ["expo-libghostty"],
  "devDependencies": {
    "@libsql/client": "^0.17.4",
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.10",
    "drizzle-orm": "^0.45.2",
    "typescript": "^7.0.2"
  },
  "workspaces": ["apps/*", "packages/*"],
  "patchedDependencies": {
    "@expo/ui@57.0.14": "patches/@expo%2Fui@57.0.14.patch",
    "@mattermost/react-native-paste-input@2.0.1": "patches/@mattermost%2Freact-native-paste-input@2.0.1.patch",
    "expo-libghostty@0.8.1": "patches/expo-libghostty@0.8.1.patch"
  }
}
