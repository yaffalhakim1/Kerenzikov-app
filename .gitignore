target
.waku-cache
.DS_Store
*.log
.env

/dist
node_modules
temp/

# Local tooling state: the code knowledge-graph index and the agent config
# directory (which installs its own node_modules).
.codebase-memory/
.opencode/

# Per-session agent memory, not project source.
MEMORY.md

.DS_Store
