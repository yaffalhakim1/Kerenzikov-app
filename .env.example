# Optional. Overrides the baked-in Windows update feed, so a moved or broken
# feed can be diagnosed or recovered from without cutting a release. Must be an
# https:// URL; the app's Ed25519 signature stays the trust root.
WAKU_FEED_URL=https://example.invalid/appcast-windows-x86_64.xml
